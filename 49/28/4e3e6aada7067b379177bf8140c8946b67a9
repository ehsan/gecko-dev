
copyrev: 9170bbe64d2fbe72279a7ee3b4afc23002a3e447
copy: js/src/trace-test/tests/closures/incr-exit-3.js

actual = '';
expected = 'foo4,foo4,';

v = 0
let(f = function (y) {
    let(f = function (y) {
        let(f = function (g) {
            for (h in g) {
                if (++y > 3) {
                    appendToActual('foo' + y)
                }
            }
        }) {
            f([y
            for (b in [1, []])]);
            f(['', false])
        }
        v = String
    }) {
        f(y)
        f(y)
    }
}) {
    f(v)
}


assertEq(actual, expected)
