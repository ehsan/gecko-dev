
copy: js/src/jit-test/tests/debug/debug-object-27.js
copyrev: 3169ec8731d7078f9a8adf8ce79615af625df0c2

// |jit-test| debug
// frame.type throws if !frame.live

load(libdir + "asserts.js");

var g = newGlobal('new-compartment');
g.debuggeeGlobal = this;
g.eval("var f;");
g.eval("(" + function () {
        Debug(debuggeeGlobal).hooks = {
            debuggerHandler: function (frame) {
                assertEq(frame.type, "call");
                assertEq(frame.live, true);
                f = frame;
            }
        };
    } + ")()");

(function () { debugger; })();
assertEq(g.f.live, false);
assertThrowsInstanceOf(function () { g.f.type; }, g.Error);
