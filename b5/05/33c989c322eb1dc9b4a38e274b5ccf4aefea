
copy: js/src/jit-test/tests/parallelarray/throw-never-executed.js
copyrev: afba103605da013f6a9196cfb4489537d7c530d1

load(libdir + "parallelarray-helpers.js");

function inc(n) {
  if (n > 1024)
    throw n;
  return n + 1;
}

if (getBuildConfiguration().parallelJS)
  assertArraySeqParResultsEq(range(0, 512), "map", inc);
